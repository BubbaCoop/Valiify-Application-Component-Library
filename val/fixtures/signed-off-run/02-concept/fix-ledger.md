# Fix ledger — primary-contact v1 → v2 (against 03-critique-1.md)

| finding id | block | change made | § |
| --- | --- | --- | --- |
| F1 | b05 / b06 / b07 | **Fixed (blocking).** Web main: `w-67` dropped from b05 (bag is now `flex gap-6`) and added to b06 and b07. Mobile main: `md:w-67` dropped from b05 (bag is now `flex flex-col gap-5 md:flex-row md:gap-6`) and added to b06 and b07. Block labels restated so the row carries direction + gap only and each field carries the 268; concept.md's mapping rows for b05 / b06 / b07 now say the same thing the markup does. | §4 "Two-up fields"; §11 "Two-up fields"; skill §7 `data-class` |
| F2 | b05 | **Partly applied, and the residual is declared.** The file's notation is now stated explicitly in concept.md Unsure: each `<main>` declares the bag resolved for its own viewport (b02, b13, b11, b12), and the only `md:` prefixes are on b05 / b06 / b07 in the mobile main, recording §1.2's single stacking rule. The prefixes are not removed because F1's prescribed fix places `md:w-67` on b06 / b07 in the mobile main verbatim; where the two findings pull opposite ways the blocking one wins. | skill §7 `data-class`; §1.2 |
| F3 | b02 (mobile) | **Fixed.** `w-[343px]` deleted. The shell and the column are now two declared elements: b02 carries the canvas fill only, b13 carries `p-4 flex flex-col gap-4` on the 375 shell, and the label states that 343 is the result of the padding, not a declared width. | §1.2; Appendix A "Dimensions" |
| F4 | b02 (+ new b13) | **Fixed.** §11's two things are now two blocks: **b02** `shell` = canvas, `bg-surface-app-page` only (both viewports); **b13** `shell` = body column, web `w-140 mx-auto py-12 flex flex-col gap-10`, mobile `p-4 flex flex-col gap-4`. The fill can no longer land on the 560 column, so the page either side of it is painted. New id b13 (next integer); b01–b12 unchanged, none renumbered, none removed. | §11 "Page shell"; §9.2 "never pure white" |
| F5 | b03 | **Fixed, choice recorded.** The eyebrow is de-cased to `Step 3 of 10 / Primary contact` in both mains, with `type-eyebrow` supplying the caps — one casing convention across the file, matching c13 / c14. Copy table c20 updated (typed form + rendered form) and the choice recorded in Unsure so the build does not flip it. | §0 casing; §8 "casing is a transform" |
| F6 | b01 | **Fixed.** Copy row **c21** added — role "header language label", text `English` (web) / `EN` (mobile below `md`), source **methodology** §1.1 / §1.2 — and `data-copy="c21"` added to b01 in both mains, with `data-copy-id="c21"` on the label line. Noted in Unsure as sample copy the build must not invent. | §1.1; §1.2; skill §5 |
| F7 | b05 | **No change — none required; the critique says so.** The "one reversible block" flag stays in Unsure as written, extended only by naming the frame (172:10673) and the exact reversal (delete the b05 wrapper, drop `w-67` / `md:w-67`, no other class change). The reviewer settles it. | §1.2; §4; §11 |
| F8 | b12 / b13 (mobile) | **No padding invented — recorded only, as instructed.** Unsure now carries the unspecified bottom offset under the 76px sticky footer, stating that §1.2 gives no offset and §13 lists none, so no value may be inferred; if the build needs one it is a new question. A matching note sits in b12's block label. | §1.2; §13 |
| F9 | copy table c08–c10 | **Fixed.** The §8 cell for c08, c09, c10 (and c11, same defect) now cites the labels/placeholders rule — a placeholder is not a repeat of the label — and the format claim is dropped; the format rule stays cited only on c12, the one shaped field. Strings untouched. | §8 "placeholders show the format" |
| F10 | concept.md | **Fixed.** A one-line **Responsive behaviour** section added after Planned values, pointing at concept section 3 (Mobile behaviour per §1.2), so the section list reads complete against skill §7. | skill §7 section order |
